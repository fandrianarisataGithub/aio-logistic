<form method="post" action="{{ path('tran_position_pneu_delete', {'id': tran_position_pneu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tran_position_pneu.id) }}">
    <button class="btn btn-danger btn-xs">Supprimer</button>
</form>
