{% extends 'base_transport.html.twig' %}

{% block title %}TransMarquePneu{% endblock %}

{% block body %}
    <h1>Marque pneu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trans_marque_pneu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ trans_marque_pneu.nom }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ trans_marque_pneu.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trans_marque_pneu_index') }}" class="btn btn-primary btn-xs">Revenir à la liste</a>

    <div class="d-flex my-4">
    <a href="{{ path('trans_marque_pneu_edit', {'id': trans_marque_pneu.id}) }}" class="btn btn-xs btn-success mr-2">Modifier</a>

    {{ include('trans_marque_pneu/_delete_form.html.twig') }}
    </div>
{% endblock %}
