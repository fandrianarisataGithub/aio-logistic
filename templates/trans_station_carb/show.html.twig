{% extends 'base_transport.html.twig' %}

{% block title %}TransStationCarb{% endblock %}

{% block body %}
    <h1>Station Carburant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trans_station_carb.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ trans_station_carb.nom }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ trans_station_carb.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trans_station_carb_index') }}" class="btn btn-primary btn-xs">Revenir à la liste</a>

    <div class="d-flex my-4">
        <a href="{{ path('trans_station_carb_edit', {'id': trans_station_carb.id}) }}" class="btn btn-xs btn-success mr-2">edit</a>

        {{ include('trans_station_carb/_delete_form.html.twig') }}
    </div>
{% endblock %}
