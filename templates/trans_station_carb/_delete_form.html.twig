<form method="post" action="{{ path('trans_station_carb_delete', {'id': trans_station_carb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trans_station_carb.id) }}">
    <button class="btn btn-xs btn-danger">Supprimer</button>
</form>
