<div class="mt-4">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-xs btn-success">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
</div>
