{% extends 'base_transport.html.twig' %}

{% block title %}TransReferencePneu{% endblock %}

{% block body %}
    <h1>Référence pneu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trans_reference_pneu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ trans_reference_pneu.nom }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ trans_reference_pneu.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trans_reference_pneu_index') }}" class="btn btn-primary btn-xs my-4">Revenir à la liste</a>

    <div class="d-flex mt-4">
        <a href="{{ path('trans_reference_pneu_edit', {'id': trans_reference_pneu.id}) }}" class="btn btn-xs btn-success mr-2">Modifier</a>

    {{ include('trans_reference_pneu/_delete_form.html.twig') }}
    </div>
{% endblock %}
