{% extends 'base_transport.html.twig' %}

{% block title %}TransClientVoyageCarb{% endblock %}

{% block body %}
    <h1>Client voyage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trans_client_voyage_carb.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ trans_client_voyage_carb.nom }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ trans_client_voyage_carb.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trans_client_voyage_carb_index') }}" class="btn btn-primary btn-xs">Revenir à la liste</a>

    <div class="d-flex my-4">
        <a href="{{ path('trans_client_voyage_carb_edit', {'id': trans_client_voyage_carb.id}) }}" class="btn btn-success btn-xs mr-2">Modifier</a>

        {{ include('trans_client_voyage_carb/_delete_form.html.twig') }}
    </div>
{% endblock %}
