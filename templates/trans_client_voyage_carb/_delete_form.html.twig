<form method="post" action="{{ path('trans_client_voyage_carb_delete', {'id': trans_client_voyage_carb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trans_client_voyage_carb.id) }}">
    <button class="btn btn-danger btn-xs">Supprimer</button>
</form>
